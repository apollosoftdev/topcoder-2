# ===========================================
# Backend Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# -------------------------------------------
# Required Variables
# -------------------------------------------

# Database connection string
# Use 'postgres' as host in devcontainer, 'localhost' for local development
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/guardrails

# nosemgrep: ai.generic.detect-generic-ai-anthprop.detect-generic-ai-anthprop
# AI Provider: "anthropic", "gemini", or "groq"
AI_PROVIDER=groq

# Groq API Key (get from https://console.groq.com/keys) - FREE!
GROQ_API_KEY=your-groq-api-key

# Google Gemini API Key (get from https://aistudio.google.com/apikey)
# GOOGLE_API_KEY=your-google-api-key

# nosemgrep: ai.generic.detect-generic-ai-anthprop.detect-generic-ai-anthprop
# Anthropic API Key (get from https://console.anthropic.com/settings/keys)
# nosemgrep: ai.generic.detect-generic-ai-anthprop.detect-generic-ai-anthprop
# ANTHROPIC_API_KEY=your-anthropic-api-key

# GitHub App webhook secret (must match GITHUB_WEBHOOK_SECRET in github-app)
GITHUB_APP_SECRET=your-webhook-secret

# -------------------------------------------
# Optional Variables (have defaults)
# -------------------------------------------

# API key for service-to-service auth (must match BACKEND_API_KEY in github-app)
# API_KEY=optional-api-key

# Debug mode
DEBUG=false

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# nosemgrep: ai.generic.detect-generic-ai-anthprop.detect-generic-ai-anthprop
# Claude model configuration (if using Anthropic)
# nosemgrep: ai.generic.detect-generic-ai-anthprop.detect-generic-ai-anthprop
CLAUDE_MODEL=claude-sonnet-4-20250514
CLAUDE_MAX_TOKENS=4096

# Gemini model configuration (if using Gemini)
GEMINI_MODEL=gemini-2.0-flash

# Groq model configuration (if using Groq)
GROQ_MODEL=llama-3.3-70b-versatile

# Analysis settings
MAX_DIFF_SIZE=100000
DEFAULT_ENFORCEMENT_MODE=warning
